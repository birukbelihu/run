package main

const (
	AppName          = "run"
	AppVersion       = "0.0.1"
	AppDescription   = "compile & run source file of any language with just one command"
	AppVersionString = AppName + " Version " + AppVersion
	Examples         = "e.g. main.go, app.py, Main.java, index.js etc."
)
